package br.ufsc.ine.leb.sistemaBancario.testes;

import org.hamcrest.*;

import br.ufsc.ine.leb.sistemaBancario.*;

public class ÉConta extends BaseMatcher<Conta>
{
	private final String id, titular, banco, agência;
	
	public ÉConta(String id, String titular, String banco, String agência)
	{
		// TODO Auto-generated constructor stub
		Conta c = new SistemaBancario().criarBanco(null, null).criarAgencia(null).criarConta(null);
		this.id = id;
		this.titular = titular;
		this.banco = banco;
		this.agência = agência;
	}
	
	@Override
	public boolean matches(Object obj)
	{
		if (!(obj instanceof Conta))
			return false;
		Conta c = (Conta) obj;
		Agencia a = c.obterAgencia();
		
		return c.obterIdentificador().equals(id)
				&& c.obterTitular().equals(titular)
				&& a.obterBanco().obterNome().equals(banco)
				&& a.obterNome().equals(agência);
	}

	@Override
	public void describeTo(Description arg0)
	{
		
	}

}
